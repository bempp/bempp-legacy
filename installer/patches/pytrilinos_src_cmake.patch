--- ./CMakeLists.txt	2013-10-22 17:55:43.000000000 +0100
+++ ./CMakeLists.txt.new	2013-11-20 12:22:17.000000000 +0000
@@ -62,7 +62,7 @@ TRIBITS_CONFIGURE_FILE(PyTrilinos_config
 # to the pytrilinos library and PyTrilinos extension modules
 SET(EXTRA_LINK_ARGS "${CMAKE_SHARED_LINKER_FLAGS}")
 IF(APPLE)
-  IF((CMAKE_CXX_COMPILER_ID MATCHES) "GNU" OR (CMAKE_CXX_COMPILER_ID MATCHES "Clang"))
+  IF((CMAKE_CXX_COMPILER_ID MATCHES "GNU") OR (CMAKE_CXX_COMPILER_ID MATCHES "Clang"))
     SET(EXTRA_LINK_ARGS "${EXTRA_LINK_ARGS} -undefined dynamic_lookup")
   ENDIF()
 ENDIF(APPLE)
